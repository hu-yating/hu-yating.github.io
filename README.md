# Ya Ting Hu Portfolio

This Hugo portfolio now uses the Blowfish theme via Hugo Modules.

## Local development

Install Hugo Extended and Go, then run:

```sh
hugo mod get
hugo server
```

Blowfish is configured in `config/_default/module.toml`.

